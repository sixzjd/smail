# smail environment configuration
PORT=8787
DOMAIN=localhost
ADMIN=admin@localhost
JWT_SECRET=smail-secret-change-me-in-production

# SMTP configuration (optional, for sending emails externally)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=

# Data directory (default: ./data relative to project root)
# DATA_DIR=./data

# Debug options
# ORM_LOG=false
# ANALYSIS_CACHE=false
